{
  "name": "granite-cloudflare-mcp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute granite-accounts --remote --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute granite-accounts --local --file=schema.sql",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "gray-matter": "^4.0.3",
    "hono": "^4.0.0",
    "js-yaml": "^4.1.0",
    "nanoid": "^5.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.79.0"
  }
}
